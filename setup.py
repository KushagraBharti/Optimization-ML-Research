# setup.py

from setuptools import setup, find_packages

setup(
    name="coverage_planning",
    version="0.1.0",
    author="Kushagra Bharti",
    description="Oracles and baselines for 1D drone-coverage planning",
    packages=find_packages(),
    python_requires=">=3.12.7",
    install_requires=[
    ],
)
